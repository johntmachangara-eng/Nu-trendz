// Placeholder for when connected to a database later
document.addEventListener("DOMContentLoaded", () => {
  const reviewsContainer = document.getElementById("reviewsContainer");

  // In the future, you'll fetch from your database:
  // fetch("/api/reviews").then(res => res.json()).then(data => renderReviews(data));
});
